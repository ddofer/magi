NOT DELIVERABLES - audit reference only.

fig5c_MAT1A_transcript_alignment_ensembl116_livepage.{pdf,png}
    Browser capture of the live Ensembl 116 transcript-comparison page, kept so the
    recomputed panel 5c can be checked against the real page. It contains embedded
    rasters and is NOT print quality.

The deliverable for panel 5c is ../fig5c_MAT1A_transcript_alignment.{pdf,svg,png}
(true vector, recomputed from the Ensembl REST API - see ../fig5bc_PROVENANCE.md
for why the live page could no longer be used directly).
